version: '3.8'

services:
  nightreign-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nightreign-dashboard
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
